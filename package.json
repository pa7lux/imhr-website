{
  "name": "imhr-website",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "prettier --check './**/*.{js,html,css}'",
    "format": "prettier --write './**/*.{js,html,css}' && postcss --no-map --replace '**/*.css'"
  },
  "devDependencies": {
    "postcss": "^8.4.14",
    "postcss-cli": "^10.0.0",
    "postcss-sorting": "^7.0.1",
    "prettier": "2.7.1",
    "vite": "^3.1.0"
  }
}
